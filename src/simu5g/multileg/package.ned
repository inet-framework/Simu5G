//
// Multileg demo project for Simu5G.
//
// Emulates an EXTERNAL project built on the multi-leg seams: a UE with extra
// NR stack legs (leg indices >= 2), each attached to its own gNB. Everything
// lives in this package and simulations/multileg; no stock Simu5G module is
// modified, and deleting the two directories restores a stock tree.
//
package simu5g.multileg;
