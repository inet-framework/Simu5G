---
phase: 02-full-bler-table-cqi-feedback-consistency
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.h
  - src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.cc
autonomous: false
requirements: [MOD-03]
user_setup: []

must_haves:
  truths:
    - "SionnaChannelModel::isReceptionSuccessful sources its block error rate from SionnaTable::lookupBler keyed by the scheduler's MCS (via cqiToMcsIndex), not from binder_->phyPisaData.getBler."
    - "The uniform(0,1) <= effectiveErrorRateWithHarq draw and the harqReduction_ multiply are retained verbatim on top of the Sionna BLER."
    - "A CQI of 0 still returns reception-failed (the existing cqi==0 guard), and a CQI mapping below MCS 2 is handled (no inf, no over-read)."
  artifacts:
    - path: "src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.cc"
      provides: "isReceptionSuccessful override sourcing BLER from lookupBler"
      contains: "lookupBler"
  key_links:
    - from: "SionnaChannelModel::isReceptionSuccessful"
      to: "SionnaTable::lookupBler"
      via: "cqiToMcsIndex(cqi,DL) → lookupBler({linkIndex,2,mcs,snr})"
      pattern: "lookupBler"
    - from: "SionnaChannelModel::isReceptionSuccessful"
      to: "harqReduction_ draw"
      via: "per * pow(harqReduction_, attempt-1); uniform(0,1) <= ..."
      pattern: "harqReduction_"
---

<objective>
Complete MOD-03: override `isReceptionSuccessful` in `SionnaChannelModel` so the block error rate
comes from `SionnaTable::lookupBler` (keyed by the MCS the scheduler assigned, via the shared
`cqiToMcsIndex` bridge from plan 02) instead of `binder_->phyPisaData.getBler(itxmode, cqi, snr)`,
while keeping the `uniform(0,1) <= BLER` success draw and the `harqReduction_` HARQ heuristic verbatim
on top. Only the BLER *source* changes; all downstream reception math is copied unchanged.

Purpose: Third vertical slice — after this plan reception decisions in a feature-ON run are driven by
the Sionna per-MCS BLER table. This is the reader half of "one table, two readers"; plan 04 adds the
feedback reader and the self-consistency gate.

Output: extended SionnaChannelModel (`.h` override decl + `.cc` body), feature-ON build green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-full-bler-table-cqi-feedback-consistency/02-CONTEXT.md
@.planning/phases/02-full-bler-table-cqi-feedback-consistency/02-RESEARCH.md
@.planning/phases/02-full-bler-table-cqi-feedback-consistency/02-PATTERNS.md
@src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.h
@src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.cc
@src/simu5g/stack/phy/channelmodel/LteRealisticChannelModel.cc
@src/simu5g/stack/mac/amc/NrAmc.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Override isReceptionSuccessful — swap BLER source to lookupBler, keep draw + harq (MOD-03)</name>
  <files>src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.h, src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.cc</files>
  <read_first>
    - src/simu5g/stack/phy/channelmodel/LteRealisticChannelModel.cc (isReceptionSuccessful lines 1709-1848: header/CQI extraction 1713-1763, cqi==0 guard 1781, cqi>15 throw, getBler call 1796, success product / harq 1801-1817, uniform draw 1838)
    - src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.cc (existing initialize, linkKeyFor lines 51-, getAttenuation lines 74-75 showing sionnaManager_->getTable() usage)
    - src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.h (class decl line 28, sionnaManager_ line 33, linkKeyFor line 40 — add the override there)
    - src/simu5g/stack/mac/amc/NrAmc.h (cqiToMcsIndex signature from plan 02)
    - .planning/phases/02-full-bler-table-cqi-feedback-consistency/02-PATTERNS.md (SionnaChannelModel section: the override structure, lines to copy verbatim)
    - .planning/phases/02-full-bler-table-cqi-feedback-consistency/02-RESEARCH.md (Code Examples: Reception swap; Pitfall 2 low-CQI/inf handling; Pitfall 5 harqReduction_)
  </read_first>
  <action>
    Declare `bool isReceptionSuccessful(LteAirFrame *frame, UserControlInfo *lteInfo) override;` in `SionnaChannelModel.h` (near `linkKeyFor`). In `SionnaChannelModel.cc`, implement it by copying the inherited body's header/CQI extraction verbatim (LteRealisticChannelModel.cc lines 1713-1763), keeping the `cqi == 0 → return false` guard (line 1781) and the `cqi > 15 → throw cRuntimeError` guard. Replace ONLY the BLER source (line 1796): instead of `binder_->phyPisaData.getBler(itxmode, cqi, snr)`, compute `int mcs = nrAmc->cqiToMcsIndex(cqi, DL);` (the shared bridge from plan 02 — obtain the NrAmc the same way Simu5G's AMC is reached, or via the binder/cell info as the base does) and `double blockErrorRate = sionnaManager_->getTable().lookupBler({ linkKeyFor(nodeId,dir,coord), /*mcsTableIndex=*/2, mcs, (double)snr });`. If the CQI maps below MCS mcsMin_ (2), treat it as the existing cqi==0 path (reception-failed / no usable MCS) rather than calling lookupBler with an out-of-range key (Pitfall 2 — no inf, no over-read). Keep EVERYTHING downstream verbatim (Pitfall 5): `blockSuccessRate = 1 - blockErrorRate`, `pow(blockSuccessRate, allocation)`, `cumulativeSuccessProbability`, `packetErrorRate`, `effectiveErrorRateWithHarq = packetErrorRate * pow(harqReduction_, transmissionAttempt - 1)`, `receptionFailed = uniform(0,1) <= effectiveErrorRateWithHarq`. Do NOT re-derive EESM (D-07) and do NOT drop harqReduction_. Add the degenerate-S note where snr is passed (v2 SINR-bin hook). NOTE: this plan does NOT touch the feedback factory or the D-08 self-check (plan 04).
  </action>
  <verify>
    <automated>bash tests/sionna/unit/run_unit_tests.sh 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'isReceptionSuccessful' src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.h` (override declared).
    - `grep -q 'lookupBler' src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.cc` and `grep -q 'cqiToMcsIndex' src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.cc`.
    - `grep -q 'harqReduction_' src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.cc` and `grep -Eq 'uniform\(0' src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.cc` (draw + harq retained).
    - `grep -vq 'phyPisaData.getBler' src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.cc` (the analytic BLER source is NOT used in the override).
    - Feature-ON build compiles; `bash tests/sionna/unit/run_unit_tests.sh` → 0 failures.
  </acceptance_criteria>
  <done>Reception BLER comes from SionnaTable::lookupBler keyed by the scheduler's MCS; the uniform draw + harqReduction_ are unchanged; low-CQI/inf is handled without over-read.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify — feature-ON single-link run uses Sionna reception BLER</name>
  <action>Pause for human verification of the MOD-03 reception swap after Task 1's automation lands: build feature-ON, regenerate the artifact, run the Sionna single-link config, and confirm reception is driven by the Sionna BLER table without cRuntimeError or chronic 100% failure (steps in how-to-verify).</action>
  <what-built>SionnaChannelModel::isReceptionSuccessful now sources block error rate from the Sionna BLER table (MOD-03), keeping the uniform(0,1) draw + harqReduction_.</what-built>
  <how-to-verify>
    1. Build feature-ON: enable the Simu5G_Sionna feature and `make` (the Sionna C++ skeleton + new code compile).
    2. Regenerate the artifact: `/home/zoli/Projects/OMNET/Sionna/venv/bin/python tools/sionna_precompute/precompute.py tools/sionna_precompute/scenario.example.json --out <artifact_dir>` (now emits bler.bin + extended manifest).
    3. Run the Sionna single-link config (the Phase-1 end-to-end config) feature-ON.
    4. Confirm the run completes without cRuntimeError (contract assert + bler.bin load pass) and produces reception outcomes (0.sca). Confirm there is no chronic 100%-failure (which would indicate an inf/low-CQI mishandle).
  </how-to-verify>
  <resume-signal>Type "approved" once the feature-ON single-link run completes and reception uses the Sionna BLER, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UserTxParams CQI (on-the-wire) → isReceptionSuccessful | CQI 1-15 from the scheduler; must map to a valid in-range MCS before lookup. |
| SionnaTable::lookupBler return → reception draw | BLER must be a finite probability before feeding uniform(0,1) <= per. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | CQI mapping below MCS 2 → out-of-range lookupBler key (over-read / inf) | mitigate | Map low CQI to the existing cqi==0 reception-failed path instead of querying lookupBler with an out-of-range MCS (Pitfall 2); lookupBler bounds-checks as defense-in-depth (plan 02). |
| T-02-08 | Information disclosure / correctness | Two divergent CQI↔MCS mappings between reception and feedback | mitigate | Reception uses the SAME `NrAmc::cqiToMcsIndex` helper plan 04's feedback uses (D-02 single bridge). |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase. |
</threat_model>

<verification>
- `bash tests/sionna/unit/run_unit_tests.sh` → 0 failures.
- Feature-ON build green; feature-ON single-link run completes without cRuntimeError (checkpoint).
- Reception sources BLER from lookupBler (grep), not phyPisaData.getBler.
</verification>

<success_criteria>
- MOD-03 satisfied: reception BLER from SionnaTable lookup, keyed by the scheduler's MCS, with the uniform(0,1) draw + harqReduction_ retained.
- One table, two readers: reception uses the shared bridge (consistent with plan 04's feedback).
</success_criteria>

<artifacts_produced>
New / changed symbols:
- `SionnaChannelModel::isReceptionSuccessful` override (MOD-03) — reception reader of the shared table.
</artifacts_produced>

<output>
Create `.planning/phases/02-full-bler-table-cqi-feedback-consistency/02-03-SUMMARY.md` when done.
</output>
