---
phase: 02-full-bler-table-cqi-feedback-consistency
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - src/simu5g/stack/phy/channelmodel/sionna/SionnaFeedbackComputation.h
  - src/simu5g/stack/phy/channelmodel/sionna/SionnaFeedbackComputation.cc
  - src/simu5g/stack/phy/LtePhyEnb.cc
  - src/simu5g/stack/phy/LtePhyEnb.ned
  - src/simu5g/stack/phy/channelmodel/sionna/SionnaManager.h
  - src/simu5g/stack/phy/channelmodel/sionna/SionnaManager.cc
  - tests/sionna/unit/test_bler_lookup.cc
  - tests/fingerprint/sionna_singlelink.csv
autonomous: false
requirements: [FB-01]
user_setup: []

must_haves:
  truths:
    - "SionnaFeedbackComputation::getCqi selects the highest MCS (2-27) whose SionnaTable BLER <= targetBler_ (exact, no epsilon, D-09) from the SAME table, then maps MCS→CQI via the shared bridge (D-02)."
    - "The hardcoded feedback factory (LtePhyEnb::initializeFeedbackComputation) instantiates SionnaFeedbackComputation when an active SionnaManager is present, else the unchanged LteFeedbackComputationRealistic (Open Q 2)."
    - "SionnaFeedbackComputation receives the channel model's targetBler_ (single source, D-09 / Open Q 3) — verified that no second targetBler silently diverges."
    - "At init SionnaManager runs the D-08 self-consistency check across all links: the feedback-selected MCS's table BLER is <= targetBler_, aborting with cRuntimeError on any violation (does NOT assume the top MCS is reachable, Pitfall 3)."
  artifacts:
    - path: "src/simu5g/stack/phy/channelmodel/sionna/SionnaFeedbackComputation.h"
      provides: "SionnaFeedbackComputation : LteFeedbackComputationRealistic, getCqi override (FB-01)"
      contains: "getCqi"
    - path: "src/simu5g/stack/phy/channelmodel/sionna/SionnaManager.cc"
      provides: "D-08 self-consistency check across links"
      contains: "targetBler"
  key_links:
    - from: "LtePhyEnb::initializeFeedbackComputation"
      to: "SionnaFeedbackComputation"
      via: "dynamic_cast SionnaManager present → new SionnaFeedbackComputation(...)"
      pattern: "SionnaFeedbackComputation"
    - from: "SionnaFeedbackComputation::getCqi"
      to: "SionnaTable::lookupBler"
      via: "highest mcs where lookupBler(...) <= targetBler_ → mcsIndexToCqi"
      pattern: "lookupBler"
    - from: "SionnaManager::initialize"
      to: "D-08 self-check"
      via: "for each link: lookupBler(selected mcs) <= targetBler_ else cRuntimeError"
      pattern: "targetBler"
---

<objective>
Complete FB-01: add `SionnaFeedbackComputation` (subclass of `LteFeedbackComputationRealistic`)
whose `getCqi` selects the highest MCS with table BLER <= `targetBler_` from the SAME `SionnaTable`
and maps it to a CQI via the shared `mcsIndexToCqi` bridge (D-02); wire it into the hardcoded
feedback factory in `LtePhyEnb` gated on an active `SionnaManager` (Open Q 2); ensure it is
constructed with the channel model's single `targetBler_` (D-09 / Open Q 3); and add the D-08 init
self-consistency check in `SionnaManager` that aborts (`cRuntimeError`) if any link's feedback-selected
MCS has realized BLER > `targetBler_`. Then re-pin the Sionna fingerprint baseline (reception now
consumes BLER) and human-verify the controlled-link consistency (success criterion 4).

Purpose: Final vertical slice — after this plan the scheduler's chosen MCS and the realized BLER
provably agree (one table, two readers), enforced fail-loud at init. Resolves Open Questions 2 and 3.

Output: new SionnaFeedbackComputation, factory patch + NED knob, D-08 self-check, extended unit test,
re-pinned fingerprint baseline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-full-bler-table-cqi-feedback-consistency/02-CONTEXT.md
@.planning/phases/02-full-bler-table-cqi-feedback-consistency/02-RESEARCH.md
@.planning/phases/02-full-bler-table-cqi-feedback-consistency/02-PATTERNS.md
@.planning/phases/02-full-bler-table-cqi-feedback-consistency/02-VALIDATION.md
@src/simu5g/stack/phy/feedback/LteFeedbackComputationRealistic.h
@src/simu5g/stack/phy/feedback/LteFeedbackComputationRealistic.cc
@src/simu5g/stack/phy/LtePhyEnb.cc
@src/simu5g/stack/phy/channelmodel/sionna/SionnaManager.cc
@src/simu5g/stack/mac/amc/NrAmc.h
@tests/sionna/unit/test_bler_lookup.cc
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SionnaFeedbackComputation getCqi override — highest MCS with BLER <= targetBler_ (FB-01, D-09)</name>
  <files>src/simu5g/stack/phy/channelmodel/sionna/SionnaFeedbackComputation.h, src/simu5g/stack/phy/channelmodel/sionna/SionnaFeedbackComputation.cc, tests/sionna/unit/test_bler_lookup.cc</files>
  <read_first>
    - src/simu5g/stack/phy/feedback/LteFeedbackComputationRealistic.h (class decl lines 21-60: members, getCqi protected decl, constructor) and .cc (constructor line 24; getCqi lines 83-106: the targetBler_ argmin loop to mirror)
    - src/simu5g/stack/mac/amc/NrAmc.h (mcsIndexToCqi from plan 02)
    - src/simu5g/stack/phy/channelmodel/sionna/SionnaTable.h (BlerQuery, lookupBler from plan 02)
    - .planning/phases/02-full-bler-table-cqi-feedback-consistency/02-PATTERNS.md (SionnaFeedbackComputation section: header, getCqi override, constructor — exact shapes)
    - .planning/phases/02-full-bler-table-cqi-feedback-consistency/02-RESEARCH.md (Pattern 3 getCqi; Pitfall 3 top-MCS floor — likely selects MCS 26 not 27)
    - tests/sionna/unit/test_bler_lookup.cc (extend with the getCqi selection + D-08 pure-logic tests)
  </read_first>
  <behavior>
    - getCqi over a fixture table picks the HIGHEST mcs in [mcsMin_,mcsMax_] with lookupBler(...) <= targetBler_ (exact <=, no epsilon).
    - When no MCS satisfies BLER <= targetBler_, getCqi returns 0 (matches base "below quality" → CQI 0).
    - At the empty-world ~70 dB fixture where MCS 27 BLER ~0.15 > 0.01 but MCS 26 ~0.0007 <= 0.01, getCqi selects MCS 26 → its CQI (NOT MCS 27 — test must not hard-code 27, Pitfall 3).
    - D-08 pure logic: for the selected MCS, lookupBler(...) <= targetBler_ holds (the check is near-tautological by construction; a violation = wiring bug).
  </behavior>
  <action>
    Create `SionnaFeedbackComputation.{h,cc}` as a subclass of `LteFeedbackComputationRealistic` (PATTERNS.md exact shapes). Header: members `const SionnaTable *table_; std::size_t linkId_; int mcsTableIndex_; int mcsMin_; int mcsMax_;`; protected `Cqi getCqi(TxMode txmode, double snr) override;`; constructor `SionnaFeedbackComputation(Binder*, double targetBler, unsigned numBands, const SionnaTable*, std::size_t linkId, int mcsTableIndex, int mcsMin, int mcsMax)` chaining the base ctor and storing the extras. `getCqi`: iterate `for (int mcs = mcsMin_; mcs <= mcsMax_; ++mcs)` and keep `best = mcs` whenever `table_->lookupBler({linkId_, mcsTableIndex_, mcs, snr}) <= targetBler_` (exact comparison on the identical stored double — D-09, NO epsilon); return `0` if `best < 0`, else `mcsIndexToCqi(best, DL)` (the shared inverse bridge from plan 02 — D-02). Do NOT assume the top MCS (27) is reachable (Pitfall 3). Extend `test_bler_lookup.cc` with the four behaviors above, exercising getCqi on a fixture table that reproduces the MCS-26-vs-27 floor.
  </action>
  <verify>
    <automated>bash tests/sionna/unit/run_unit_tests.sh 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'class SionnaFeedbackComputation' src/simu5g/stack/phy/channelmodel/sionna/SionnaFeedbackComputation.h` and `grep -q 'getCqi' SionnaFeedbackComputation.h`.
    - `grep -q 'lookupBler' src/simu5g/stack/phy/channelmodel/sionna/SionnaFeedbackComputation.cc` and `grep -q 'mcsIndexToCqi' SionnaFeedbackComputation.cc`.
    - No `epsilon`/`1e-` fudge in the `<= targetBler_` comparison (`grep -vEq 'targetBler_ \+|epsilon' SionnaFeedbackComputation.cc` for the comparison line).
    - No test hard-codes selected MCS == 27.
    - `bash tests/sionna/unit/run_unit_tests.sh` → 0 failures.
  </acceptance_criteria>
  <done>SionnaFeedbackComputation::getCqi selects the highest MCS with BLER <= targetBler_ from the shared table and maps to CQI; unit tests green; top-MCS floor handled.</done>
</task>

<task type="auto">
  <name>Task 2: Factory wiring + single targetBler_ + D-08 self-consistency check (FB-01, Open Q 2 & 3, D-08)</name>
  <files>src/simu5g/stack/phy/LtePhyEnb.cc, src/simu5g/stack/phy/LtePhyEnb.ned, src/simu5g/stack/phy/channelmodel/sionna/SionnaManager.h, src/simu5g/stack/phy/channelmodel/sionna/SionnaManager.cc</files>
  <read_first>
    - src/simu5g/stack/phy/LtePhyEnb.cc (initializeFeedbackComputation lines 340-353 — the hardcoded `new LteFeedbackComputationRealistic(...)` at 348; getFeedbackComputationFromName 320-335; note `targetBler = par("targetBler")` at 344 — this is LtePhyEnb's NED targetBler, the Open Q 3 divergence vs the channel model's 0.01)
    - src/simu5g/stack/phy/LtePhyEnb.ned (targetBler default — confirm value; Open Q 3)
    - src/simu5g/stack/phy/channelmodel/sionna/SionnaManager.h (.cc) (initialize stage + getTable + where targetBler_ could be read; the LiveContract)
    - src/simu5g/stack/mac/amc/NrAmc.h (cqiToMcsIndex/mcsIndexToCqi for the self-check)
    - .planning/phases/02-full-bler-table-cqi-feedback-consistency/02-PATTERNS.md (factory-wiring patch; SionnaManager D-08 self-check step 5)
    - .planning/phases/02-full-bler-table-cqi-feedback-consistency/02-RESEARCH.md (Open Q 2 recommendation: gate on SionnaManager presence; Open Q 3 / A4: use the channel model's targetBler_; Pitfall 1; Pitfall 3)
  </read_first>
  <action>
    1. Factory (Open Q 2, Pitfall 1): in `LtePhyEnb::initializeFeedbackComputation`, resolve the active `SionnaManager` (via a NED module-path param on LtePhyEnb, e.g. `sionnaManagerModule = default("")`, dynamic_cast the resolved module). If present, build `new SionnaFeedbackComputation(binder_, targetBler, numBands, &sm->getTable(), linkId, 2, 2, 27)`; else keep the existing `new LteFeedbackComputationRealistic(...)` unchanged (no behavior change when Sionna is off — SEAM-02). Add the NED param to `LtePhyEnb.ned`.
    2. Single targetBler_ (Open Q 3 / D-09 / A4): the `targetBler` passed to the Sionna feedback computation MUST be the channel model's `targetBler_` (default 0.01), NOT LtePhyEnb's NED `targetBler` (which research found defaults to 0.001) and NOT the factory literal 0.1. Read the channel model's value (from the resolved SionnaChannelModel / SionnaManager) and pass THAT single double. Add a one-line assert/comment proving the value used equals the channel model's targetBler_ so no second targetBler silently diverges. CITE D-09.
    3. D-08 self-check (in SionnaManager::initialize, after bler.bin load — the step deferred from plan 02): for each link, compute the feedback-selected MCS exactly as getCqi does (highest mcs with `lookupBler({link,2,mcs,sinr}) <= targetBler_`), then assert that selected MCS's BLER `<= targetBler_`; on any link where it does not hold, `throw cRuntimeError("SionnaManager self-consistency: link %zu feedback MCS %d has BLER %g > targetBler_ %g", ...)` (CAL-02 style, one throw per failing link). Use the channel model's `targetBler_` (same single double). Do NOT additionally assert the TOP MCS (27) is reachable (Pitfall 3) — only the selected MCS. The check is near-tautological by construction; a violation signals a real two-readers-disagree bug (D-08).
  </action>
  <verify>
    <automated>bash tests/sionna/unit/run_unit_tests.sh 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'SionnaFeedbackComputation' src/simu5g/stack/phy/LtePhyEnb.cc` and the instantiation is gated on a non-null SionnaManager (`grep -q 'dynamic_cast' LtePhyEnb.cc`).
    - When no SionnaManager is present, the factory still does `new LteFeedbackComputationRealistic` (default build unaffected — `grep -q 'LteFeedbackComputationRealistic' LtePhyEnb.cc`).
    - `grep -q 'sionnaManagerModule' src/simu5g/stack/phy/LtePhyEnb.ned`.
    - SionnaManager.cc contains the self-check throwing on `BLER > targetBler_` (`grep -Eq 'targetBler_|targetBler' src/simu5g/stack/phy/channelmodel/sionna/SionnaManager.cc` with a self-consistency cRuntimeError).
    - The self-check does NOT assert "MCS == 27 reachable".
    - `bash tests/sionna/unit/run_unit_tests.sh` → 0 failures; feature-ON build compiles.
  </acceptance_criteria>
  <done>The feedback factory instantiates SionnaFeedbackComputation when Sionna is active using the channel model's single targetBler_; the D-08 init self-check fails loud on any link disagreement; default build unaffected.</done>
</task>

<task type="auto">
  <name>Task 3: Re-pin Sionna fingerprint baseline (BLER now drives reception, REP-01 intent)</name>
  <files>tests/fingerprint/sionna_singlelink.csv</files>
  <read_first>
    - tests/fingerprint/sionna_singlelink.csv (the Phase-1 path-gain-only baseline now stale)
    - .planning/phases/02-full-bler-table-cqi-feedback-consistency/02-RESEARCH.md (Pitfall 6: re-capture via fingerprinttest.py; PHYAbstraction deterministic; pin bler.bin as source of truth)
    - .planning/phases/01-thin-end-to-end-slice-bring-up-empty-world-validation/01-04-SUMMARY.md (how the Phase-1 baseline was captured — fingerprinttest.py invocation)
  </read_first>
  <action>
    After plans 03 + 04's reception/feedback wiring lands and the feature-ON single-link config runs green, re-capture the Sionna single-link fingerprint baseline via `fingerprinttest.py` (the same mechanism Phase 1 used) — the Sionna config's OWN baseline, never matched to the analytic model's (REP-01 intent). Commit the regenerated `bler.bin`-backed artifact as the reproducible source of truth alongside the updated CSV. Confirm a second feature-ON run reproduces the pinned fingerprint (PHYAbstraction is deterministic — Pitfall 6).
  </action>
  <verify>
    <human-check>Run the Sionna single-link fingerprint test twice feature-ON; confirm both runs match the re-pinned baseline.</human-check>
  </verify>
  <acceptance_criteria>
    - `tests/fingerprint/sionna_singlelink.csv` is updated (differs from the Phase-1 path-gain-only baseline) and is the Sionna config's own baseline.
    - Two consecutive feature-ON fingerprint runs produce the identical pinned value (determinism).
    - The committed artifact (`bler.bin` + manifest) is the pinned reproducible source.
  </acceptance_criteria>
  <done>The Sionna single-link fingerprint baseline is re-pinned against the BLER-driven reception and reproduces deterministically.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify — controlled-link MCS/BLER agreement (Phase 2 success criterion 4)</name>
  <action>Pause for human verification of FB-01 consistency after Tasks 1-3 land: run the controlled single-link config feature-ON, confirm the D-08 self-check passes at init, and inspect scalars to confirm the feedback-selected MCS's realized BLER tracks targetBler_ with no chronic over-target BLER (steps in how-to-verify).</action>
  <what-built>SionnaFeedbackComputation::getCqi + the D-08 self-check make the scheduler's chosen MCS and the realized BLER provably agree from one shared SionnaTable.</what-built>
  <how-to-verify>
    1. Build feature-ON and run the Sionna single-link config with the regenerated artifact (bler.bin present).
    2. Confirm init passes the D-08 self-consistency check (no cRuntimeError) — the feedback-selected MCS per link has table BLER <= targetBler_.
    3. Inspect the scalars (`opp_scavetool`/0.sca): confirm the feedback-selected MCS's realized BLER tracks targetBler_ (no chronic BLER far above target from table disagreement) — Phase 2 success criterion 4.
    4. Confirm the selected MCS is the expected one (likely 26, not 27, at empty-world SINR — Pitfall 3) and document it.
  </how-to-verify>
  <resume-signal>Type "approved" once the controlled-link run shows scheduler MCS and realized BLER moving together with no chronic over-target BLER, or describe the discrepancy.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LtePhyEnb NED targetBler vs channel model targetBler_ | Two different targetBler defaults exist (0.001 vs 0.01 vs 0.1); the wrong one breaks the D-09 single-source premise. |
| SionnaManager init → D-08 self-check | The self-check must read the SAME targetBler_ both readers use, else it passes/fails spuriously. |
| feedback factory → SionnaFeedbackComputation | Must only activate when Sionna is on, leaving the default build path byte-for-byte unchanged (SEAM-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering / correctness | A second targetBler (0.001/0.1) silently feeds the feedback computation, breaking single-source (D-09) | mitigate | Construct SionnaFeedbackComputation and the self-check with the channel model's targetBler_; assert/comment proving equality (Open Q 3 / A4). |
| T-02-10 | Tampering | Feedback factory activates Sionna path in a default build (SEAM-02 regression) | mitigate | Instantiation gated on a non-null SionnaManager; absent → unchanged LteFeedbackComputationRealistic (default build unaffected). |
| T-02-11 | Spoofing / correctness | Two readers disagree (table or bridge mismatch) → chronic over-target BLER | mitigate | D-08 init self-check aborts fail-loud (cRuntimeError) if any link's feedback-selected MCS BLER > targetBler_ (CAL-02 style). |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase. |
</threat_model>

<verification>
- `bash tests/sionna/unit/run_unit_tests.sh` → 0 failures (getCqi selection + D-08 pure logic green).
- Feature-ON build green; feature-ON single-link run passes the D-08 self-check at init.
- Default build path unchanged when no SionnaManager present (factory grep).
- Re-pinned Sionna fingerprint reproduces deterministically across two runs (checkpoint).
- Controlled-link scalars show MCS/BLER agreement (human-verify checkpoint).
</verification>

<success_criteria>
- FB-01 satisfied: getCqi selects highest MCS with BLER <= targetBler_ from the SAME table (D-09 exact); init self-check confirms agreement (D-08).
- Open Q 2 resolved (factory gated on SionnaManager presence) and Open Q 3 resolved (single channel-model targetBler_).
- Phase 2 success criterion 4 demonstrated: scheduler MCS and realized BLER move together.
- Default build byte-for-byte unaffected (SEAM-02 preserved).
</success_criteria>

<artifacts_produced>
New / changed symbols:
- `SionnaFeedbackComputation` (.h/.cc) + `getCqi` override (FB-01) — the feedback reader of the shared table.
- `LtePhyEnb::initializeFeedbackComputation` factory patch + `sionnaManagerModule` NED param (Open Q 2).
- `SionnaManager` D-08 self-consistency check (deferred from plan 02) using the single channel-model targetBler_ (Open Q 3).
- Re-pinned `tests/fingerprint/sionna_singlelink.csv` (REP-01 intent).
</artifacts_produced>

<output>
Create `.planning/phases/02-full-bler-table-cqi-feedback-consistency/02-04-SUMMARY.md` when done.
</output>
