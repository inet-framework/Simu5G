---
phase: 01-thin-end-to-end-slice-bring-up-empty-world-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .oppfeatures
  - src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.h
  - src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.cc
  - src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.ned
  - src/simu5g/stack/phy/channelmodel/sionna/SionnaManager.h
  - src/simu5g/stack/phy/channelmodel/sionna/SionnaManager.cc
  - src/simu5g/stack/phy/channelmodel/sionna/SionnaManager.ned
  - src/simu5g/stack/phy/channelmodel/sionna/SionnaTable.h
  - src/simu5g/stack/phy/channelmodel/sionna/SionnaTable.cc
  - src/simu5g/stack/phy/channelmodel/sionna/ManifestReader.h
  - src/simu5g/stack/phy/channelmodel/sionna/ManifestReader.cc
  - tests/sionna/check_default_build_symbols.sh
autonomous: true
requirements: [SEAM-02]
user_setup: []

must_haves:
  truths:
    - "A default `opp_makemake --deep` build links zero Sionna/HDF5/Python symbols (the Sionna sources are excluded by a disabled .oppfeatures feature)"
    - "Enabling the Simu5G_Sionna feature compiles the new sionna source folder and links the new classes"
    - "A symbol-check script proves the default binary is free of sionna/hdf5/python symbols"
  artifacts:
    - path: ".oppfeatures"
      provides: "Simu5G_Sionna feature (initiallyEnabled=false) excluding the sionna source folder from the default deep build"
      contains: "Simu5G_Sionna"
    - path: "src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.h"
      provides: "SionnaChannelModel : public NrChannelModel with getAttenuation override declaration"
    - path: "src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.ned"
      provides: "simple SionnaChannelModel extends NrChannelModel @class(SionnaChannelModel) (SEAM-01 registration)"
    - path: "tests/sionna/check_default_build_symbols.sh"
      provides: "SEAM-02 default-binary symbol-check gate"
  key_links:
    - from: ".oppfeatures"
      to: "src/simu5g/stack/phy/channelmodel/sionna"
      via: "extraSourceFolders excludes the folder from default deep build"
      pattern: "extraSourceFolders.*sionna"
    - from: "SionnaChannelModel.ned"
      to: "NrChannelModel.ned"
      via: "extends NrChannelModel (satisfies like ILteChannelModel via parent chain)"
      pattern: "extends NrChannelModel"
---

<objective>
Establish the build-isolation seam (SEAM-02) and the compilable C++ skeleton for the Sionna channel model. Add a `Simu5G_Sionna` `.oppfeatures` feature (`initiallyEnabled="false"`) whose `extraSourceFolders` excludes the new `sionna/` source directory from the default deep build, mirroring `Simu5G_Cars`. Create the four C++ class pairs as compilable skeletons (`SionnaChannelModel`, `SionnaManager`, `SionnaTable`, `ManifestReader`) plus their NED, and a symbol-check script proving the default binary links zero Sionna/HDF5/Python symbols.

Purpose: SEAM-02 is the load-bearing constraint of the whole project — the default build/run must stay byte-for-byte unaffected. This plan proves the isolation seam works before any real Sionna C++ logic exists, and provides the skeleton files that Plan 01-03 fills in. It touches the C++ side only and shares no files with Plan 01-01, so the two run in parallel.
Output: `.oppfeatures` feature, the `sionna/` skeleton source tree, and `tests/sionna/check_default_build_symbols.sh`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-thin-end-to-end-slice-bring-up-empty-world-validation/01-RESEARCH.md
@.planning/phases/01-thin-end-to-end-slice-bring-up-empty-world-validation/01-PATTERNS.md
@.planning/phases/01-thin-end-to-end-slice-bring-up-empty-world-validation/01-SKELETON.md
@.oppfeatures
@src/simu5g/stack/phy/channelmodel/NrChannelModel.ned
@src/simu5g/stack/phy/channelmodel/NrChannelModel.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Simu5G_Sionna build-isolation feature + symbol-check gate (SEAM-02)</name>
  <files>.oppfeatures, tests/sionna/check_default_build_symbols.sh</files>
  <read_first>
    - .oppfeatures (existing Simu5G_Cars feature + root cppSourceRoots/definesFile attrs)
    - .planning/phases/01-thin-end-to-end-slice-bring-up-empty-world-validation/01-PATTERNS.md (.oppfeatures section — exact feature XML)
    - .planning/phases/01-thin-end-to-end-slice-bring-up-empty-world-validation/01-RESEARCH.md (Pitfall 1; "Default-build isolation feature")
  </read_first>
  <action>Add a `<feature>` element to `.oppfeatures` with `id="Simu5G_Sionna"`, `name="Simu5G Sionna"`, `description="Site-specific channel model from offline Sionna RT precompute"`, `initiallyEnabled="false"`, `requires=""`, `labels=""`, `nedPackages="simu5g.stack.phy.channelmodel.sionna"`, `extraSourceFolders="src/simu5g/stack/phy/channelmodel/sionna"`, `compileFlags="-DWITH_SIONNA"`, `linkerFlags=""`. The `extraSourceFolders` value is what excludes the folder from the default deep build (the default build only compiles folders NOT claimed by a disabled feature). Then create `tests/sionna/check_default_build_symbols.sh`: a bash script that runs `nm -D` (or `nm` if `-D` yields nothing for a static archive) on the default-built binary/library (`bin/simu5g` and/or `bin/libsimu5g.so` / `out/.../libsimu5g*.so`), pipes through `grep -iE 'sionna|hdf5|python|tensorflow|torch'`, and exits nonzero if ANY match is found (SEAM-02 gate). The script must resolve the binary path robustly and print which file it checked.</action>
  <verify>
    <automated>grep -q "Simu5G_Sionna" .oppfeatures && grep -q 'extraSourceFolders.*channelmodel/sionna' .oppfeatures && test -x tests/sionna/check_default_build_symbols.sh && bash -n tests/sionna/check_default_build_symbols.sh && echo FEATURE-OK</automated>
  </verify>
  <acceptance_criteria>
    - `.oppfeatures` contains a `Simu5G_Sionna` feature with `initiallyEnabled="false"` and `extraSourceFolders` pointing at `src/simu5g/stack/phy/channelmodel/sionna`.
    - `grep -c 'Simu5G_Sionna' .oppfeatures` is >= 1.
    - `tests/sionna/check_default_build_symbols.sh` is executable, passes `bash -n` syntax check, and greps for `sionna|hdf5|python|tensorflow|torch`.
  </acceptance_criteria>
  <done>The Simu5G_Sionna feature isolates the new source folder from the default build; the SEAM-02 symbol-check gate exists and is syntactically valid.</done>
</task>

<task type="auto">
  <name>Task 2: Compilable C++ + NED skeleton for the four Sionna classes</name>
  <files>src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.h, src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.cc, src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.ned, src/simu5g/stack/phy/channelmodel/sionna/SionnaManager.h, src/simu5g/stack/phy/channelmodel/sionna/SionnaManager.cc, src/simu5g/stack/phy/channelmodel/sionna/SionnaManager.ned, src/simu5g/stack/phy/channelmodel/sionna/SionnaTable.h, src/simu5g/stack/phy/channelmodel/sionna/SionnaTable.cc, src/simu5g/stack/phy/channelmodel/sionna/ManifestReader.h, src/simu5g/stack/phy/channelmodel/sionna/ManifestReader.cc</files>
  <read_first>
    - src/simu5g/stack/phy/channelmodel/NrChannelModel.h (header pattern: include guard, single-base inheritance, getAttenuation override signature)
    - src/simu5g/stack/phy/channelmodel/NrChannelModel.cc (Define_Module + initialize base-chaining)
    - src/simu5g/stack/phy/channelmodel/NrChannelModel.ned (extends + @class minimal form)
    - .planning/phases/01-thin-end-to-end-slice-bring-up-empty-world-validation/01-PATTERNS.md (all four class sections + "No Analog Found")
  </read_first>
  <action>Create `src/simu5g/stack/phy/channelmodel/sionna/` with: (1) `SionnaChannelModel.{h,cc,ned}` — `class SionnaChannelModel : public NrChannelModel` in namespace `simu5g`, include guard `SIONNACHANNELMODEL_H_`, license header copied verbatim from `NrChannelModel.h:1-11`, declaring `void initialize(int stage) override;` and `double getAttenuation(MacNodeId nodeId, Direction dir, inet::Coord coord, bool cqiDl) override;`. The `.cc` has `Define_Module(SionnaChannelModel);`, `initialize` calls `NrChannelModel::initialize(stage)` first, and a STUB `getAttenuation` body that for now `return NrChannelModel::getAttenuation(nodeId, dir, coord, cqiDl);` with a `// TODO(01-03): replace with -table_->lookup(...)` marker. The `.ned`: `package simu5g.stack.phy.channelmodel.sionna; import simu5g.stack.phy.channelmodel.NrChannelModel; simple SionnaChannelModel extends NrChannelModel { parameters: @class("SionnaChannelModel"); string sionnaManagerModule = default("^.^.sionnaManager"); string artifactManifest = default("sionna_artifact/manifest.json"); }`. (2) `SionnaManager.{h,cc,ned}` — `class SionnaManager : public omnetpp::cSimpleModule` with `void initialize(int stage) override;` stub gated on `inet::INITSTAGE_LOCAL`, `Define_Module(SionnaManager);`, NED `simple SionnaManager { parameters: @class("SionnaManager"); string artifactManifest = default("sionna_artifact/manifest.json"); }`. (3) `SionnaTable.{h,cc}` — plain class (no Define_Module) holding `std::vector<double> pathGainDb_`, methods `static SionnaTable loadBinary(const std::string& path, size_t numLinks)` (stub) and `double lookup(size_t linkIndex) const`. (4) `ManifestReader.{h,cc}` — plain class with a `struct Manifest` (fields: `int schema_version; double carrier_frequency_hz; double subcarrier_spacing_hz; int num_bands; std::string table_path; std::string table_dtype; size_t num_links; /* coord_transform + request_hash placeholders */`) and `static Manifest read(const std::string& path)` (stub returning a default-constructed Manifest). Guard all `.cc` bodies that include Sionna-only headers behind `#ifdef WITH_SIONNA` is NOT required (the whole folder is feature-excluded), but keep includes minimal. Copy the license header into every new file. Goal: the folder compiles cleanly WHEN the feature is enabled, and is fully ignored when disabled.</action>
  <verify>
    <automated>test -f src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.h && grep -q "class SionnaChannelModel : public NrChannelModel" src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.h && grep -q "double getAttenuation(MacNodeId" src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.h && grep -q "Define_Module(SionnaChannelModel)" src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.cc && grep -q '@class("SionnaChannelModel")' src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.ned && grep -q "extends NrChannelModel" src/simu5g/stack/phy/channelmodel/sionna/SionnaChannelModel.ned && test -f src/simu5g/stack/phy/channelmodel/sionna/ManifestReader.h && test -f src/simu5g/stack/phy/channelmodel/sionna/SionnaTable.h && test -f src/simu5g/stack/phy/channelmodel/sionna/SionnaManager.ned && echo SKELETON-OK</automated>
  </verify>
  <acceptance_criteria>
    - All ten C++ files plus three NED files exist under `src/simu5g/stack/phy/channelmodel/sionna/`.
    - `SionnaChannelModel.h` declares `class SionnaChannelModel : public NrChannelModel` and the `getAttenuation` override with the exact base signature `(MacNodeId, Direction, inet::Coord, bool)`.
    - `SionnaChannelModel.ned` contains `extends NrChannelModel` and `@class("SionnaChannelModel")` (SEAM-01 registration).
    - `SionnaManager.cc` contains `Define_Module(SionnaManager)`; `SionnaTable`/`ManifestReader` are plain (no Define_Module).
    - Every new file carries the Simu5G license header.
  </acceptance_criteria>
  <done>The four Sionna classes exist as compilable skeletons with correct inheritance, module registration, and NED, ready for Plan 01-03 to fill in the loader and override.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build system → default binary | A misconfigured feature could pull Sionna sources into the default build, breaking SEAM-02 and (later) adding HDF5/Python deps. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | default build silently compiling sionna/ sources | mitigate | `.oppfeatures` `extraSourceFolders` excludes the folder when the feature is disabled; `tests/sionna/check_default_build_symbols.sh` asserts zero sionna/hdf5/python symbols (Pitfall 1). |
| T-02-02 | Denial of Service | feature pulling a heavy dep into a normal build | mitigate | No external lib is linked by the skeleton; the binary+JSON reader (Plan 01-03) is hand-rolled with no HDF5/Python dep. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan; nothing to verify. |
</threat_model>

<verification>
- `.oppfeatures` has the Simu5G_Sionna feature; `extraSourceFolders` points at the sionna folder.
- The symbol-check script exists, is executable, and is syntactically valid.
- All skeleton files exist with correct inheritance, `@class`, and `Define_Module` placement.
- (Wave-merge) A default build (feature OFF) followed by `tests/sionna/check_default_build_symbols.sh` finds no sionna/hdf5/python symbols; a feature-ON build compiles the folder.
</verification>

<success_criteria>
- The Simu5G_Sionna feature isolates the new source folder from the default deep build (SEAM-02).
- The C++ skeleton compiles when the feature is enabled and is ignored when disabled.
- The symbol-check gate is ready to prove the default binary is Sionna/HDF5/Python-free.
- Requirement SEAM-02 is demonstrably exercised.
</success_criteria>

<output>
Create `.planning/phases/01-thin-end-to-end-slice-bring-up-empty-world-validation/01-02-SUMMARY.md` when done.
</output>
