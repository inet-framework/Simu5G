---
phase: 01-thin-end-to-end-slice-bring-up-empty-world-validation
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - simulations/NR/sionna/omnetpp.ini
  - simulations/NR/sionna/SionnaSingleLink.ned
  - simulations/NR/sionna/package.ned
  - tests/fingerprint/sionna_singlelink.csv
autonomous: false
requirements: [SEAM-01, MOD-01, CAL-01]
user_setup: []

must_haves:
  truths:
    - "Setting nrChannelModelType = \"SionnaChannelModel\" in the ini (no NED interface edit) instantiates the Sionna model and runs a single-link simulation end-to-end"
    - "The single-link empty-world run loads the artifact and produces rcvdSinrDl/path gain derived from the Sionna table, not the analytic formula"
    - "The in-sim path gain matches the offline Friis round-trip value within tolerance, closing the validation loop"
    - "A pinned fingerprint baseline exists for the Sionna single-link config"
  artifacts:
    - path: "simulations/NR/sionna/omnetpp.ini"
      provides: "Single-link Sionna run config: ini selection + Phase-1 statistical-term suppression"
      contains: "SionnaChannelModel"
    - path: "simulations/NR/sionna/SionnaSingleLink.ned"
      provides: "Single-link network (one gNB + one UE) for the empty-world slice"
    - path: "tests/fingerprint/sionna_singlelink.csv"
      provides: "Pinned fingerprint baseline for the Sionna single-link config"
  key_links:
    - from: "simulations/NR/sionna/omnetpp.ini"
      to: "NrNicUe.ned parametric typename nrChannelModelType"
      via: "ini string override selects SionnaChannelModel (no NED edit) — SEAM-01"
      pattern: "nrChannelModelType.*SionnaChannelModel"
    - from: "simulations/NR/sionna/omnetpp.ini"
      to: "tools/sionna_precompute artifact (manifest.json + path_gain.bin)"
      via: "artifactManifest path consumed by SionnaManager at init"
      pattern: "artifactManifest"
---

<objective>
Close the walking-skeleton loop end-to-end: author a single-link NR network (one gNB + one UE) and an ini config that selects `SionnaChannelModel` purely via the `nrChannelModelType` parametric-typename string (no NED interface edit — SEAM-01), suppresses the Phase-1 statistical terms (`shadowing=false`, `fading=false`, `fixedLos=true` — Assumption A3), points `artifactManifest` at the artifact produced by Plan 01-01, runs the empty-world simulation so the per-link path gain comes from Sionna (MOD-01), and verifies the in-sim path gain matches the offline Friis round-trip (CAL-01). Pin a fingerprint baseline.

Purpose: This is the integration slice that proves a researcher can select and run the Sionna model end-to-end and trust it via the empty-world Friis round-trip. It depends on the loaded C++ consumer (01-03), so it is Wave 3. It includes a human-verify checkpoint to confirm the simulation runs and the round-trip closes.
Output: `simulations/NR/sionna/{omnetpp.ini, SionnaSingleLink.ned, package.ned}` and a pinned `tests/fingerprint/sionna_singlelink.csv`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-thin-end-to-end-slice-bring-up-empty-world-validation/01-RESEARCH.md
@.planning/phases/01-thin-end-to-end-slice-bring-up-empty-world-validation/01-PATTERNS.md
@.planning/phases/01-thin-end-to-end-slice-bring-up-empty-world-validation/01-SKELETON.md
@.planning/phases/01-thin-end-to-end-slice-bring-up-empty-world-validation/01-01-SUMMARY.md
@.planning/phases/01-thin-end-to-end-slice-bring-up-empty-world-validation/01-03-SUMMARY.md
@src/simu5g/stack/NrNicUe.ned
</context>

<tasks>

<task type="auto">
  <name>Task 1: Single-link network + ini selecting SionnaChannelModel via the parametric typename (SEAM-01, MOD-01)</name>
  <files>simulations/NR/sionna/SionnaSingleLink.ned, simulations/NR/sionna/package.ned, simulations/NR/sionna/omnetpp.ini</files>
  <read_first>
    - src/simu5g/stack/NrNicUe.ned (parametric typename nrChannelModelType at lines ~33,71 — the SEAM-01 mechanism)
    - .planning/phases/01-thin-end-to-end-slice-bring-up-empty-world-validation/01-PATTERNS.md (omnetpp.ini section — exact ini override + suppression lines)
    - .planning/phases/01-thin-end-to-end-slice-bring-up-empty-world-validation/01-01-SUMMARY.md (artifact output path the ini must point artifactManifest at)
    - simulations/NR/standalone (an existing single-cell standalone NR network to mirror for the gNB+UE layout)
  </read_first>
  <action>Create a minimal NR single-link network `SionnaSingleLink.ned` (`package simu5g.simulations.nr.sionna;` plus `package.ned`) with one gNB and one UE placed at the SSOT positions matching the artifact (Tx at [0,0,10], Rx at [100,0,1.5]) so the OMNeT++ Euclidean distance is 100 m. Mirror an existing standalone NR network for the binder/channelControl/gNB/UE scaffolding. Create `omnetpp.ini` with: `*.*.cellularNic.nrChannelModelType = "SionnaChannelModel"` (SEAM-01 — ini string only, NO NED interface edit), the Phase-1 inert-statistical-term lines `*.*.cellularNic.nrChannelModel[*].shadowing = false`, `*.*.cellularNic.nrChannelModel[*].fading = false`, `*.*.cellularNic.nrChannelModel[*].dynamicLos = false`, `*.*.cellularNic.nrChannelModel[*].fixedLos = true` (Pitfall 3 / A3), and the `artifactManifest` parameter pointing at the artifact produced by Plan 01-01 (default `sionna_artifact/manifest.json`; document how to generate it via `tools/sionna_precompute/precompute.py`). Add a `SionnaManager` instance in the network (or NIC scope) so `SionnaChannelModel`'s `sionnaManagerModule` resolves. Keep the run short (single TTI / a few seconds sim time) to make fingerprinting fast.</action>
  <verify>
    <automated>grep -q 'nrChannelModelType.*"SionnaChannelModel"' simulations/NR/sionna/omnetpp.ini && grep -q "shadowing = false" simulations/NR/sionna/omnetpp.ini && grep -q "fixedLos = true" simulations/NR/sionna/omnetpp.ini && grep -q artifactManifest simulations/NR/sionna/omnetpp.ini && test -f simulations/NR/sionna/SionnaSingleLink.ned && echo CONFIG-OK</automated>
  </verify>
  <acceptance_criteria>
    - omnetpp.ini selects SionnaChannelModel via nrChannelModelType (ini string only) and sets shadowing/fading=false, fixedLos=true.
    - omnetpp.ini sets artifactManifest pointing at the Plan 01-01 artifact.
    - SionnaSingleLink.ned places the gNB and UE 100 m apart matching the artifact link.
    - No NED interface (ILteChannelModel.ned / NrNicUe.ned) is edited (SEAM-01 preserved).
  </acceptance_criteria>
  <done>A single-link NR network + ini select and run SionnaChannelModel via the parametric typename with statistical terms suppressed and the artifact wired in.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the end-to-end slice and confirm the empty-world Friis round-trip closes (CAL-01, MOD-01)</name>
  <files>simulations/NR/sionna/omnetpp.ini, simulations/NR/sionna/sionna_artifact/manifest.json</files>
  <what-built>
    A feature-ON Simu5G build (Simu5G_Sionna enabled), the offline-generated artifact, and a single-link NR config that runs SionnaChannelModel end-to-end. The in-sim per-link path gain comes from the Sionna table; the offline friis_check.py already proved the Sionna value matches Friis within ~0.04 dB.
  </what-built>
  <read_first>
    - simulations/NR/sionna/omnetpp.ini (the config just authored)
    - .planning/phases/01-thin-end-to-end-slice-bring-up-empty-world-validation/01-01-SUMMARY.md (how to regenerate the artifact)
    - .planning/phases/01-thin-end-to-end-slice-bring-up-empty-world-validation/01-VALIDATION.md (full-suite command)
  </read_first>
  <action>Manual operator run (no autonomous code change): execute the how-to-verify steps below in order to confirm the end-to-end slice runs on the Sionna path gain and the Friis round-trip closes in-sim, including the corrupted-manifest negative check. This task gates the wave; do not auto-approve.</action>
  <how-to-verify>
    1. Generate the artifact: `/home/zoli/Projects/OMNET/Sionna/venv/bin/python tools/sionna_precompute/precompute.py tools/sionna_precompute/scenario.example.json --out simulations/NR/sionna/sionna_artifact`.
    2. Enable the feature and build: enable `Simu5G_Sionna` (via the IDE feature manager or `opp_featuretool enable Simu5G_Sionna`), then `make` from `src/`.
    3. Run the single-link config: `cd simulations/NR/sionna && opp_run -u Cmdenv -c General` (or the project's standard run command) and confirm it completes without a cRuntimeError.
    4. Confirm the recorded per-link downlink path gain / rcvdSinrDl reflects the Sionna value (~-83.3 dB path gain at 100 m), i.e. it tracks the offline friis_check.py value within ~1 dB and is NOT the analytic-formula value.
    5. Negative check: edit the manifest carrier_frequency_hz to a wrong value and re-run — the run MUST abort with a cRuntimeError (CAL-02 fires).
  </how-to-verify>
  <verify>
    <human-check>Operator confirms: (a) the run completes with SionnaChannelModel active, (b) the in-sim path gain matches the offline Friis/Sionna value within ~1 dB, and (c) the corrupted-manifest re-run aborts with cRuntimeError.</human-check>
  </verify>
  <acceptance_criteria>
    - The simulation runs to completion with SionnaChannelModel active (no crash, no silent fallback).
    - The in-sim path gain matches the offline Friis/Sionna value within ~1 dB (CAL-01 loop closed end-to-end).
    - A deliberately corrupted manifest field aborts the run with cRuntimeError (CAL-02 confirmed in-sim).
  </acceptance_criteria>
  <done>The end-to-end single-link slice runs on the Sionna path gain, the empty-world Friis round-trip closes within ~1 dB, and a corrupted manifest aborts with cRuntimeError.</done>
  <resume-signal>Type "approved" once the run completes, the path gain matches the Friis round-trip, and the corrupted-manifest negative check aborts; or describe what failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Pin the Sionna single-link fingerprint baseline (REP-01 groundwork)</name>
  <files>tests/fingerprint/sionna_singlelink.csv</files>
  <read_first>
    - tests/fingerprint (existing fingerprint runner + an existing baseline csv to mirror the format)
    - simulations/NR/sionna/omnetpp.ini (the config to fingerprint)
    - .planning/phases/01-thin-end-to-end-slice-bring-up-empty-world-validation/01-VALIDATION.md ("Phase gate" — record the baseline)
  </read_first>
  <action>Run the Sionna single-link config under the fingerprint harness (with the Sionna feature ON and the artifact present) to capture a deterministic fingerprint, and record it as a NEW baseline row `tests/fingerprint/sionna_singlelink.csv` in the same column format as existing fingerprint baselines. This is the Sionna config's OWN baseline against the committed artifact (never matched to the statistical model's baselines — REP-01 intent). Document the exact command and feature/artifact preconditions in a comment or adjacent note so the baseline is reproducible. (Full REP-01/REP-02 reproducibility hardening is Phase 3; Phase 1 only records the baseline.)</action>
  <verify>
    <automated>test -s tests/fingerprint/sionna_singlelink.csv && grep -qi sionna tests/fingerprint/sionna_singlelink.csv && echo FINGERPRINT-OK</automated>
  </verify>
  <acceptance_criteria>
    - tests/fingerprint/sionna_singlelink.csv exists, is non-empty, and references the Sionna single-link config.
    - The baseline format matches existing fingerprint baselines in tests/fingerprint/.
    - The baseline is the Sionna config's own, not copied from a statistical-model baseline.
  </acceptance_criteria>
  <done>A reproducible fingerprint baseline for the Sionna single-link config is pinned, giving the empty-world slice a deterministic regression anchor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ini config → channel-model selection | the nrChannelModelType string instantiates a C++ class by name; a wrong/missing class would fail at NED resolution (a correctness risk, not a security one). |
| artifact path (ini) → SionnaManager | artifactManifest points the loader at on-disk files validated by Plan 01-03. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | mismatched artifact accepted at run time | mitigate | SionnaManager (Plan 01-03) asserts the full contract; the human-verify negative check (Task 2 step 5) confirms a corrupted manifest aborts with cRuntimeError (CAL-02). |
| T-04-02 | Tampering | artifactManifest path traversal | accept | Trusted local research config; low risk (RESEARCH Security Domain). |
| T-04-03 | Tampering | default build accidentally enabling Sionna | mitigate | Feature stays initiallyEnabled=false (Plan 01-02); this config only runs with the feature explicitly enabled; SEAM-02 symbol check (Plan 01-02) guards the default binary. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs; the artifact is produced by the pre-provisioned, pinned venv. |
</threat_model>

<verification>
- omnetpp.ini selects SionnaChannelModel via the parametric typename (no NED interface edit) and suppresses statistical terms.
- The single-link run completes with the Sionna path gain active and matches the Friis round-trip within ~1 dB (human-verify).
- A corrupted manifest aborts with cRuntimeError (human-verify negative check).
- A pinned fingerprint baseline for the Sionna config exists.
</verification>

<success_criteria>
- A researcher selects and runs SionnaChannelModel end-to-end via ini only (SEAM-01), using the Sionna per-link path gain (MOD-01), validated by the empty-world Friis round-trip (CAL-01).
- The Sionna single-link config has a pinned fingerprint baseline.
- Requirements SEAM-01, MOD-01, CAL-01 are demonstrably exercised end-to-end.
</success_criteria>

<output>
Create `.planning/phases/01-thin-end-to-end-slice-bring-up-empty-world-validation/01-04-SUMMARY.md` when done.
</output>
